init python:

    def green_mesh_top():
        name = "green mesh top"
        image_string = "green_mesh_top"
        clothing_type = "top"

        dialogue_lines = {
            }

        covers = ["breasts"]

        number_of_states = 2

        return ClothingClass(name, image_string, clothing_type, dialogue_lines, covers = covers, number_of_states = number_of_states)
